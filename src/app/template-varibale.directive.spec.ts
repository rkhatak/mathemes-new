import { TemplateVaribaleDirective } from './template-varibale.directive';

describe('TemplateVaribaleDirective', () => {
  it('should create an instance', () => {
    const directive = new TemplateVaribaleDirective();
    expect(directive).toBeTruthy();
  });
});
